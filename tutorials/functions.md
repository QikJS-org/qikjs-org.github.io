# Functions & Arrow Functions

Functions are reusable blocks of code.

```js
function greet(name) {
  return `Hello, ${name}!`;
}

// Arrow function
const add = (a, b) => a + b;

console.log(greet("QikJS"));
console.log(add(2,3)); // 5
```
